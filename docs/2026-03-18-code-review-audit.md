# Aqua Essence — Code Review Plan Audit + Security/Architecture Assessment

> **Historical snapshot (2026-03-18):** Paths and components in this audit
> describe the repository at review time. The active backend is now
> `backend/` (FastAPI); `backend2/`, the C++ host, and legacy solvers have been
> removed. Use current run and architecture documentation for present-day
> guidance.

**Date:** 2026-03-18
**Branch:** `feature/restructure-and-integrate`
**Reviewed against:** `docs/reviews/2026-03-15-code-review.md` (17 issues) and `docs/plans/2026-03-14-backlog-master-plan.md` (Task 1 gaps)

---

## Part 1: Plan Status Report

### Completed

| Plan Item                                     | Evidence                                                                                                                                                                                                                                       | Notes                                                                                            |
| --------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| **#1 XSS via innerHTML (dashboard.js)**       | `adf7991` — Added `escapeHtml()` to all innerHTML user data in dashboard.js (54 lines changed). `3466996` — Added HTML escaping in `frontend/app.js` (83 lines changed). 19 calls to `escapeHtml` in dashboard.js, 20 `esc()` calls in app.js. | Fixed in both frontends.                                                                         |
| **#2 Unvalidated JSON bodies in POST routes** | `adf7991` — "Validate POST request bodies in what-if routes (return 400)" in `frontend/xai_dashboard/app.py` (+24 lines) and `what_if.py` (+28 lines).                                                                                         | 400 errors now returned for missing/invalid fields.                                              |
| **#3 CORS wide open**                         | `adf7991` — Restricted to `CORS(app, origins=["http://localhost:5050"])` at `app.py:14`.                                                                                                                                                       | Locked to same-origin only.                                                                      |
| **#4 Flask debug=True**                       | `adf7991` — Set `debug=False` at `app.py:138`.                                                                                                                                                                                                 | Fixed.                                                                                           |
| **#5 Path traversal in DataManager**          | `adf7991` — `data_manager.py` rewritten to always load `profiles.json` from `job_dir` directly (line 26) instead of following untrusted path from result.json.                                                                                 | Traversal vector eliminated by design change.                                                    |
| **#9 Private methods called from routes**     | `adf7991` — "Make `_score_swimmer_instructor` and `_check_constraints` public" in `what_if.py`.                                                                                                                                                | Methods renamed to drop underscore.                                                              |
| **#10 Confidence scale inconsistency**        | `3466996`, `e4d98cc`, `9e6b0b3` — `formatConfidence()` added in app.js. `normalizeConfidence()` in dashboard.js handles both scales.                                                                                                           | Normalization centralized, though not yet standardized at source (C++ solver still outputs 0-1). |

### In Progress

| Plan Item                                     | Evidence                                                                                                                                                                       | Notes                                                                                                                  |
| --------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------- |
| **#8 No loading or error states (dashboard)** | `adf7991` — "Add error banner for failed API calls" in dashboard.js.                                                                                                           | Error banner added but no loading spinners during API calls. Partial fix.                                              |
| **#14 Vanilla JS duplication**                | `normalizeConfidence` and `personName` still exist only in `dashboard.js` (not shared). `esc()`/`escapeHtml()` are separately implemented in both `app.js` and `dashboard.js`. | No shared `utils.js` created yet. Duplication persists.                                                                |
| **#11 C++ backend doing Python's job**        | `backend2/server.py` (FastAPI, 590 lines) created in `fb495fb` as a Python replacement.                                                                                        | FastAPI server exists but C++ backend is still present and functional. Migration not complete — both backends coexist. |
| **#12 Two separate web servers**              | FastAPI server (`backend2/server.py`) exists but XAI dashboard is still a separate Flask app on port 5050.                                                                     | Not yet merged into single server.                                                                                     |
| **#17 Port 5050 hardcoded**                   | CORS restricted to `localhost:5050`. Port still hardcoded in `app.py`, `__main__.py`, `AquaEssenceHost.cpp` (2 locations).                                                     | No centralization via env var or config file.                                                                          |

### Not Started

| Plan Item                                             | Evidence                                                                                                                                                          | Notes                                                     |
| ----------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------- |
| **#6 No caching on `/api/match/<idx>/alternatives`**  | No caching logic found in `what_if.py` or `app.py`.                                                                                                               | No changes made.                                          |
| **#7 Overview re-fetches on every tab switch**        | `dashboard.js` still calls `loadOverview()` unconditionally on tab switch. No client-side cache in `state` object.                                                | No changes made.                                          |
| **#13 Fragile `.bat` subprocess chain**               | `solvers/python_cpsat/solver.bat` unchanged. C++ backend still shells out through batch files.                                                                    | Still 4 hops: C++ → cmd.exe → .bat → py.exe → wrapper.py. |
| **#15 Solver path resolution walks directory levels** | `solver_wrapper.py` still uses `dirname()` walk (3 levels) to find app root. No `app_root` field in request.json.                                                 | No changes made.                                          |
| **#16 Missing test coverage**                         | `test_what_if.py:64` has `test_relax_returns_new_eligible`. Still no tests for `/api/reload`, `normalizeConfidence` edge cases, or unknown `match_type` handling. | Partially addressed — moved to In Progress.               |

### Backlog Master Plan — Task 1 Gaps Status

| Gap                                     | Status        | Evidence                                                                                                 |
| --------------------------------------- | ------------- | -------------------------------------------------------------------------------------------------------- |
| Gap 1: `continuity_dispute` in frontend | **Completed** | `8af4b35` — flag serialized in `solver_wrapper.py`, displayed in `app.js` with dispute-flagged CSS class |
| Gap 2: Unassigned reason codes          | **Completed** | `93a28e7`, `cc3db1b` — `_get_unassigned_reason()` in `solver_wrapper.py:46`, reason column in frontend   |
| Gap 3: P3 Notes boosts (prefer/always)  | **Completed** | `NOTES_BOOSTS` in `config.py`, boost logic in `phase2_cpsat.py`                                          |
| Gap 4: avoid/never hard constraints     | **Completed** | `phase2_cpsat.py:275-295` — hard constraints added for individuals and pairs                             |
| Gap 5: Human-review flag styling        | **Completed** | `needs-review` and `dispute-flagged` CSS classes in `app.js` and `styles.css`                            |

---

## Part 2: Independent Security & Architecture Audit

### Critical — Exploitable now, data loss or code execution risk

| #      | Finding                                          | File : Line                           | Detail                                                                                                                                          |
| ------ | ------------------------------------------------ | ------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| ~~C1~~ | ~~**Command injection via `std::system()`**~~    | `backend/AquaEssenceHost.cpp:922-933` | **RESOLVED** — `std::system()` replaced with `CreateProcessW`. No shell interpretation of paths. Also added 30s timeout for profile generation. |
| ~~C2~~ | ~~**Deterministic job IDs enable enumeration**~~ | `backend2/server.py:238-240`          | **RESOLVED** — `generate_job_id()` now uses `secrets.token_hex(16)`, producing 128-bit random hex IDs.                                          |

### High — Likely to cause bugs or security issues under normal use

| #      | Finding                                                  | File : Line                           | Detail                                                                                                                                                                                                                                                                                                                                    |
| ------ | -------------------------------------------------------- | ------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ~~H1~~ | ~~**Fragile hand-rolled JSON parsing in C++ backend**~~  | `backend/AquaEssenceHost.cpp`         | **RESOLVED** — All hand-rolled JSON parsing (`string::find()`, brace-counting, manual unescaping) replaced with `nlohmann/json` (single-header `json.hpp`). Affected: `extract_purpose()`, `parse_named_object_string_map()`, `load_settings_from_disk()`, manifest parsing, `/api/set_solver` body parsing, and `/api/profile` endpoint. |
| ~~H2~~ | ~~**No solver process timeout**~~                        | `backend/AquaEssenceHost.cpp:338`     | **RESOLVED** — `WaitForSingleObject` now uses 5-minute timeout (`SOLVER_TIMEOUT_MS = 300000`). On timeout, `TerminateProcess` is called and an error message is returned.                                                                                                                                                                 |
| ~~H3~~ | ~~**Solver exit code never checked**~~                   | `backend/AquaEssenceHost.cpp:353-361` | **RESOLVED** — `GetExitCodeProcess()` is called at line 353-354 and non-zero codes produce an error message at line 359-361. Originally misreported as missing.                                                                                                                                                                           |
| ~~H4~~ | ~~**Race condition: timestamp-based job ID collision**~~ | `backend/AquaEssenceHost.cpp:229-248` | **RESOLVED** — Job IDs now use `UuidCreate()` instead of timestamp. Format: `job_xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx`. Eliminates collision risk.                                                                                                                                                                                        |
| ~~H5~~ | ~~**No CSRF protection on XAI dashboard**~~              | `frontend/xai_dashboard/app.py:11-21` | **RESOLVED** — `@require_json` decorator enforces `Content-Type: application/json` on all POST endpoints, returning 415 for form-encoded requests. Combined with CORS restriction, prevents cross-origin CSRF.                                                                                                                            |

### Medium — Design smell or fragility that will bite eventually

| #      | Finding                                                                    | File : Line                                                            | Detail                                                                                                                                                                                                                                                                                                                                                             |
| ------ | -------------------------------------------------------------------------- | ---------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| ~~M1~~ | ~~**Path traversal via string `startswith()` check**~~                     | `backend2/server.py:518, 552`                                          | **RESOLVED** — Replaced with `Path.is_relative_to()` (Python 3.9+).                                                                                                                                                                                                                                                                                                |
| ~~M2~~ | ~~**Unvalidated `solver_id` stored to settings**~~                         | `backend2/server.py:443-456`                                           | **RESOLVED** — `set_solver` now rejects `solver_id` containing `/`, `\`, or `..`, and validates `manifest.json` exists before saving.                                                                                                                                                                                                                              |
| ~~M3~~ | ~~**`os._exit(0)` on shutdown bypasses cleanup**~~                         | `backend2/server.py:309, 347`                                          | **RESOLVED** — Both `os._exit(0)` calls replaced with `os.kill(os.getpid(), signal.SIGINT)` for graceful uvicorn shutdown.                                                                                                                                                                                                                                         |
| M4     | **C++ backend is Windows-only**                                            | `backend/AquaEssenceHost.cpp:1-18`                                     | Entire codebase uses Win32 API (`CreateProcessW`, `GetEnvironmentVariableW`, `WaitForSingleObject`). No conditional compilation or platform abstraction. Cannot compile on Linux/macOS.                                                                                                                                                                            |
| ~~M5~~ | ~~**Solver `.bat` wrapper is Windows-only**~~                              | `solvers/*/manifest.json`, `backend2/server.py`, `AquaEssenceHost.cpp` | **RESOLVED** — Manifest entries changed from `solver.bat` to `solver_wrapper.py`. Both backends detect `.py` extension and invoke via Python interpreter (`sys.executable` / `SearchPathW`). Site-packages resolution moved into `solver_wrapper.py`. `.bat` files deleted.                                                                                        |
| ~~M6~~ | ~~**Data loader has no file I/O error handling**~~                         | `solvers/python_cpsat/engine/data_loader.py:260-270`                   | **RESOLVED** — All `pd.read_csv()` calls routed through `DataLoader._read_csv()`, which raises `FileNotFoundError` with descriptive message and regeneration hint, or `ValueError` for parse failures.                                                                                                                                                             |
| ~~M7~~ | ~~**Settings file write race**~~                                           | `backend2/server.py:119-124`                                           | **RESOLVED** — `save_settings()` now writes to `.tmp` file then calls `os.replace()` for atomic swap.                                                                                                                                                                                                                                                              |
| ~~M8~~ | ~~**TOCTOU on result.json read**~~                                         | `backend2/server.py:502-510`                                           | **RESOLVED** — Both backends now use a single guarded `result.json` read path instead of separate existence/stat checks followed by a later read. Concurrent deletion now returns a controlled 500, and the empty-file case remains explicit.                                                                                                                                                                   |
| ~~M9~~ | ~~**Error messages leak internal details**~~                               | `backend/AquaEssenceHost.cpp:1190`, `backend2/server.py:361`           | **RESOLVED** — Solver and dashboard failures are now logged server-side while API responses return generic messages instead of exposing stderr output, exit codes, or internal paths.                                                                                                                                                                                 |
| ~~M10~~ | ~~**`innerHTML` still used in `openDrawer()`**~~                          | `frontend/app.js:66`                                                   | **RESOLVED** — The profile drawer now accepts DOM nodes and uses `replaceChildren()` for content updates, so callers no longer pass raw HTML through `openDrawer()`. Profile and error rendering in the drawer is built with DOM APIs instead of `innerHTML`.                                                                                                       |
| ~~M11~~    | ~~**No persistent logging / tracking of errors, bugs and potential crashes**~~ | System-wide                                                            | **RESOLVED for the active runtime in v1** — Persistent local JSONL logging now exists under `workspace/logs/` for `backend2/server.py`, `backend/AquaEssenceHost.cpp`, `frontend/xai_dashboard/app.py`, `frontend/xai_dashboard/__main__.py`, the active Python solver wrappers, and `core/profiles/generate_profiles.py`. Logs include timestamps, correlation fields, request/job context, rotation, and 14-day retention. `L7` remains open for `solvers/cpp_exact`, which was out of scope for this pass. |

### Low — Cleanup opportunities, minor robustness improvements

| #   | Finding                                              | File : Line                                                      | Detail                                                                                                                                            |
| --- | ---------------------------------------------------- | ---------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| ~~L1~~ | ~~**`normalizeConfidence`/`escapeHtml` duplicated**~~ | `frontend/app.js` + `frontend/xai_dashboard/static/dashboard.js` | **RESOLVED** — Shared browser helpers now live in `frontend/shared/ui_utils.js`, and both the main frontend and XAI dashboard load that single source instead of maintaining duplicate `escapeHtml` / confidence-formatting logic. |
| ~~L2~~ | ~~**Hardcoded `C:\Windows\System32\cmd.exe` fallback**~~ | `backend/AquaEssenceHost.cpp:278, 1259`                          | **RESOLVED** — The C++ host now resolves the command interpreter via `COMSPEC` first and `SearchPathW("cmd.exe")` second, removing the hardcoded System32 fallback path. |
| ~~L3~~ | ~~**No rate limiting on `/api/generate`**~~        | `backend2/server.py`                                             | **RESOLVED** — Both backends now enforce a single active generation plus a short cooldown window on `/api/generate`, returning `429` with `Retry-After` instead of spawning overlapping or rapid repeat solver subprocesses. |
| ~~L4~~  | ~~**Solver path resolution via `dirname()` walk**~~      | `solvers/python_cpsat/solver_wrapper.py:100`                     | **RESOLVED** — Both backends now write an explicit `app_root` into `request.json`, and `solver_wrapper.py` resolves paths from that field with a validated fallback instead of assuming a fixed three-level directory layout.                                                      |
| ~~L5~~  | ~~**No result.json schema validation**~~                 | `backend2/server.py:508-510`                                     | **RESOLVED** — Both backends now parse and validate solver `result.json` before returning it, rejecting malformed JSON or missing contract fields with a controlled 500 instead of passing bad payloads to the frontend.                                         |
| ~~L6~~  | ~~**Mixed path separators in JSON responses**~~          | `solvers/python_cpsat/solver_wrapper.py:271`                     | **RESOLVED** — All Python solver wrappers now normalize `result_files` paths to POSIX form before writing `result.json`, so the contract no longer depends on the host OS path separator. |
| L7  | **No persistent logging**                            | `solvers/cpp_exact/solver_main.cpp:1057`                         | All debug output goes to stderr. Lost when solver process exits. No log files, timestamps, or correlation IDs.                                    |
| ~~L8~~  | ~~**Candidate port list limited to 8**~~                 | `backend2/server.py:569`                                         | **RESOLVED** — The FastAPI host now honors `PORT` when set, validates it, and otherwise falls back to an ephemeral localhost port if the preferred candidate list is exhausted. The legacy C++ host now follows the same `PORT`/fallback policy via `bind_to_any_port("127.0.0.1")`.                                                                  |
