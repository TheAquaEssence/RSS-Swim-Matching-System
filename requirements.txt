# Aqua Essence Swim Matching — runtime dependencies
# Only what the shipped backend and solver need. Development and test tools
# live in requirements-dev.txt. Pinned to the versions the app is developed
# and tested against. Python 3.14 is the supported development and desktop
# packaging interpreter; see .python-version.

ortools==9.15.6755   # CP-SAT optimizer (solvers/python_cpsat)
pandas==2.3.3        # CSV loading, transforms, and report output
fastapi==0.135.1     # Backend host server (backend/server.py)
uvicorn==0.42.0      # ASGI server for the FastAPI host
python-multipart==0.0.20  # Required by FastAPI for multipart/form-data (file upload endpoints)
jinja2==3.1.6        # XAI dashboard templates (backend/xai_router.py Jinja2Templates)
reportlab==4.4.10    # PDF report (solvers/python_cpsat/engine/pdf_report.py)
